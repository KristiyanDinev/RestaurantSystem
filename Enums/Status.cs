namespace RestaurantSystem.Enums
{
    public enum Status
    {
        Pending = 0,
        Preparing = 1,
        Ready = 2,
        Delivered = 3,
        Accepted = 4,
        Cancelled = 5,
    }
}
